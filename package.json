{
	"name": "9to5",
	"version": "0.1.7",
	"description": "Cron for Claude Code — schedule and automate Claude Code tasks",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/Michaelliv/9to5"
	},
	"homepage": "https://michaelliv.github.io/9to5/",
	"workspaces": ["packages/*"],
	"bin": {
		"9to5": "dist/cli.js"
	},
	"files": ["dist"],
	"scripts": {
		"dev": "bun run packages/cli/src/index.ts",
		"build": "bun build --compile packages/cli/src/index.ts --outfile 9to5",
		"build:publish": "bun build packages/cli/src/index.ts --outfile dist/cli.js --target bun && bun build packages/tui/src/index.tsx --outdir dist/tui --target bun --external react-devtools-core",
		"prepublishOnly": "bun run build:publish",
		"check": "bunx biome check .",
		"test": "bun test",
		"docs:dev": "vitepress dev docs",
		"docs:build": "vitepress build docs",
		"docs:preview": "vitepress preview docs"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "^1.1.0",
		"vitepress": "^1.6.4"
	}
}
